#!/bin/bash
# =============================================================================
# Hubs Local Setup for Mac
# Double-click this file to start the setup process.
# =============================================================================

# Change to the directory where this script is located
cd "$(dirname "$0")"

# Make the main script executable and run it
chmod +x setup_mac.sh
./setup_mac.sh
